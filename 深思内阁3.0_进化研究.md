# 深思内阁 3.0：从架构主张到可检验协议

## 面向前缀缓存与验证治理的编程智能体 —— 基于 2.0 论文的自我进化研究（含 Xiaomi MiMo Desktop 对照更新）

**作者**：LY（进化流程驱动：见第 3 节）
**日期**：2026-09-08
**版本**：3.0 进化版（由 2.0 中文更新版 docx 经阅读—提炼—自省—重构流程生成）

---

## 摘要

本论文是对《深思内阁 2.0：面向前缀缓存与验证治理的编程智能体架构》的自我进化研究。进化不是堆叠新功能描述，而是做三件事：第一，把 2.0 第 8 节自陈的七条缺点逐条转译为结构化工作项 W1—W7，并为每一项给出最小可验收设计；第二，补上 2.0 论证链中三处理论缺口——前缀稳定性的形式化与漂移检测、缓存—成本解析模型与自适应路由、以及验证门与只追加日志之间的一致性协议；第三，引入证据分级纪律并升格为系统第四条不变量 I4：论文与产品中每个定量主张必须标注证据级别（R0 已实现机制测试 / R1 本地受控实验 / R2 官方转述或厂商宣称 / R3 设计推演）并指向可复现来源。

进化中发现并解决了一个 2.0 未察觉的设计冲突：验证失败回退需要截断事件日志，而截断会破坏字节级前缀稳定并损失缓存收益。3.0 将 L6 的"只追加事件日志"升级为"公共提交日志 + 草稿分支"的两段式结构，验证通过才向公共日志提交——这一结构与数据库预写日志（WAL）的提交语义同构，使"缓存优先"与"验证后提交"不再互相拆台。

与 Xiaomi MiMo Desktop 的对照在本版中被降级为受控证据：厂商宣称值（如 78.6 SWE-Bench Verified、97.0% 工具成功率、最高 99% 缓存命中率）一律按 R2 外部参考值处理，仅在八坐标一致的实验卡内才允许与深思内阁的本地结果并列比较。3.0 的立场与 2.0 一致但更严：不把规划冒充为实现，也不把转述冒充为测量；但比 2.0 更进一步的是，3.0 给出了"如何让每一项主张从 R3/R2 走向 R1/R0"的可执行路径。

**关键词**：编程智能体；自我进化；前缀缓存；两段式日志；验证门；证据分级；自适应路由；多智能体；长期记忆；小米 MiMo；成本可观测性

---

## 1 引言：为什么要进化

2.0 论文已经回答过一个问题：当桌面智能体从小米这类产品把"调度、工具、成果工作区、浏览器、成本感知"整合进来时，深思内阁的差异化价值是什么。它的答案是三条不变量（字节级前缀稳定、决策输入隔离、验证后记忆）加六层架构。这个答案在方向上可信，但 2.0 论文自己在第 8 节列出的七条缺点暴露了它的真实成熟度：结论大多来自架构推导与本地模拟，缺少真实 API 实验；artifact、浏览器、并行仍停留在路线图；路由阈值是工程初值而非数据拟合结果；安全是风险降低而非可度量的边界；模型比较口径不一致；长期记忆没有长期运行数据。

3.0 的出发点很直接：**把"缺点清单"当作进化的工作清单**。一份论文说自己哪里不足，下一版就该在那些不足处给出可验收的改进，而不是换一套说法重申优点。因此 3.0 的全部新增内容都锚定在 2.0 的自陈缺点上，并接受同一条更严的自我审查纪律——本版新增的理论贡献（两段式日志、自适应路由、分层验证）在写成时仍是 R3 级设计推演，本文不把它们表述为已验证事实。

另一层进化动机是元层面的：2.0 论文描述的深思内阁——多轮审议、反方质疑、验证门、只写验证过的经验——本身是一套"让系统自我改进而不失真"的方法。3.0 把同一套方法应用于论文自身：阅读全部内容、提炼不变量与机制、以 2.0 自己的反方（第 8 节）质疑 2.0 自己、把质疑结果交给证据分级裁判、再重构输出。这个过程本身就是一次小型"内阁审议"，其记录见第 3 节。

---

## 2 基线回溯：2.0 与小米对照的要点

为避免读者回查，本节压缩保留 2.0 的关键结构，并首次执行 3.0 的证据分级标注（级别定义见 6.1 节）。

### 2.1 三条不变量与六层架构（2.0 原文）

- I1 前缀稳定：主循环请求前缀在会话内保持字节级稳定，以获得前缀缓存收益。
- I2 决策隔离：决策内阁只接收净化后的任务陈述与证据包，原始用户输入不直接进入高权限决策路径。
- I3 验证后记忆：长期记忆只写入经过验证的经验。

六层为：L1 前门路由（fast/mid/deep，按难度、影响范围、模糊度、操作风险、新颖度五维打分）；L2 信息采集内阁（证据包带来源与置信度，并净化输入）；L3 规律库（BM25 + 四级法系）；L4 决策内阁（提案、反方、复核、独立裁判）；L5 验证与记忆（正确性、完整性、安全性、约束遵守、忠实性、成本六项检查）；L6 缓存优先主循环（不可变前缀、只追加事件日志、易失草稿、工具调用修复、预算熔断、每日费用闸门）。

### 2.2 2.0 对小米公开资料的转述（全部为 R2 级）

- Xiaomi MiMo Desktop（R2，官方邀测页面，2026-09-08 访问）：Smart 调度按任务类型、复杂度与成本动态选择模型、框架与工具，复杂任务拆给多 Agent 并行；支持多格式文件作为上下文，生成 PPT、文档、网页、3D、App，预览区局部修改与版本回退；浏览器打开网页、搜索、提取、填表并自动检查交互；官方宣称同会话缓存命中率最高 99%、跨会话最高 95%。
- MiMo 模型线（R2，官方模型页与价格页，2026-09-08 访问）：MiMo-V2.5-Pro 为 1T 总参数、42B 激活、1M 上下文；MiMo-V2.5 原生理解图像/视频/音频/文本；缓存命中输入价示例 MiMo-V2.5-Pro 0.025 元/百万 token、未命中 3 元/百万 token、输出 6 元/百万 token（MiMo-V2.5 为 0.02 / 1 / 2）。
- X 预览模型（R2）：MiMo-X-Pro-Preview 与 MiMo-X-Flash-Preview 目前仅为桌面邀测模型，官方未公开稳定 API 契约、价格、缓存字段与数据保留策略，2.0 因此只保留适配器边界而不默认路由。
- 更新日志报告（R2，厂商报告值）：2026-02 的 mimo-v2-flash 更新使 Thinking 模式 SWE-Bench Verified 达 78.6、工具调用成功率达 97.0%。

2.0 的结论在此延续并被 3.0 收紧：深思内阁的差异化在缓存经济学、证据来源、输入隔离、验证门与记忆治理的显式化；小米的差异化在桌面任务覆盖与端到端成果工作流。二者互补，但任何"比较"都必须先过证据关——这正是 3.0 第 6 节协议要解决的事。

---

## 3 进化方法：把深思内阁应用于论文自身

3.0 的产生流程刻意复刻了 2.0 所描述的深思内阁循环，五个步骤与六层架构的对应如下：

1. 阅读与信息采集（对应 L2）：完整抽取 2.0 docx 全文，保留标题结构、表格、代码片段与参考文献，约 1 万字符。
2. 提炼关键信息（对应 L3 规律库）：压缩出不变量 I1—I3、六层职责、成本模型 p_eff = p_in·(h·alpha + (1 − h))、期望效用路由、九项产品指标、七条自陈缺点。
3. 反方审议（对应 L4 反方）：对每条自陈缺点追问"2.0 为什么没解决它"，得到两个层次的答案——缺实验设施（证据级别没有制度）与缺机制设计（若干机制存在内在冲突，见 5.3）。
4. 验证与裁判（对应 L5）：对反方结论逐条做可行性检查：改动是否破坏三条不变量？是否依赖未公开的 MiMo 契约？是否有可本地验收的测试形式？凡依赖 X 预览模型或厂商价格承诺的改进一律降级为 R2/R3，不进入"默认生产"表述。
5. 重构输出（对应 L6 提交）：只有通过第 4 步的改动写入 3.0；本文不把第 3 步中已被裁判否掉的方案（例如"把 X 预览模型直接设为 deep 路径默认模型"）保留为结论。

映射结果即版本差异路线图：2.0 缺点 §8.1—§8.7 → 3.0 工作项 W1—W7（第 4 节），加上三项理论深化 T1—T3（第 5 节）与证据分级纪律 I4（第 6 节）。这套"自省循环 + 版本路线图"的产物就是本论文本身，因此 3.0 的元贡献可被复用于 4.0：任何后续版本都应对上一版的自陈缺点逐条回应。

---

## 4 进化设计 A：七条缺点转译为七项工作（W1—W7）

表 1 给出映射总览，随后逐项给出最小可验收设计。其中 W1（证据分级制度）与 W6（八坐标实验卡）的展开分别见第 6.1—6.2 节与第 6.3 节；W2—W5、W7 五项如下逐项展开。

**表 1：2.0 自陈缺点 → 3.0 工作项（W1—W7）映射与验收**

| 2.0 自陈缺点 | 3.0 工作项 | 最小可验收设计 | 证据级别目标 |
| --- | --- | --- | --- |
| §8.1 缺真实 API 实验 | W1 证据分级制度 | 见 I4（第 6.1—6.2 节）：主张分级 + claim ledger 工具 | R3→R0（分级工具机制测试） |
| §8.2 功能差距显著 | W2 artifact/version 契约 v1 | manifest v1（含 contentHash/state/producedBy）+ 原子回退 | R3→R0（机制测试） |
| §8.3 并行未生产级治理 | W3 并行契约 | reads/writes 声明、写集互斥调度、合并重验 | R3→R0 |
| §8.4 阈值未校准 | W4 自适应路由 | Beta 后验在线更新 + 受预算约束的汤普森采样 | R3→R1 |
| §8.5 安全只是风险降低 | W5 净化边界与对抗测试 | 决策平面可达性审计 + 注入逃逸回归集 | R3→R0/R1 |
| §8.6 比较口径不一致 | W6 八坐标实验卡 | 见第 6.3 节；厂商值一律 R2 外部参考 | R3→R1 |
| §8.7 记忆缺长期治理 | W7 记忆生命周期 | 作用域签名 + TTL + 反事实重验 + 删除传播 | R3→R1 |

### 4.1 W2：artifact/version 契约 v1（回应 §8.2）

2.0 的成果 JSON 只有版本与回退目标，缺少可验证性与来源。v1 升级如下（字段级差异即为可验收改动）：

```json
{
  "manifestVersion": 1,
  "artifactId": "a-17",
  "kind": "file|web|document|table|image|app",
  "path": "dist/report.html",
  "contentHash": "sha256:9f2c…",
  "version": 3,
  "parentVersion": 2,
  "changedRanges": [{ "start": 120, "end": 188 }],
  "state": "committed",
  "checks": [{ "name": "html-parse", "passed": true },
             { "name": "links-ok", "passed": true }],
  "rollbackTarget": 2,
  "producedBy": { "runId": "r-9", "band": "mid",
                  "modelProfile": "standard", "cacheHit": true }
}
```

验收标准：manifest 完整性与 contentHash 校验、state 只能在 {draft → committed → superseded} 间单向迁移、回退为原子操作（version 指向 rollbackTarget 且旧版标记 superseded 保留可再回退）、diff 完整性（changedRanges 之外的字节不变）。以上全部可写成确定性机制测试，不依赖任何 LLM。

### 4.2 W3：并行契约（回应 §8.3）

每个 worker 在派发前声明 {reads: 路径集, writes: 路径集}。调度器保证并发 worker 的 writes 两两不相交；若相交则串行或分组。worker 输出只进草稿分支，合并动作由协调器执行：合并结果必须生成新 artifact 版本并重跑验证门，验证通过才提交公共日志。冲突检测指标为"合并冲突召回率"（2.0 第 7.2 节产品指标表已列，此处给出实现方式：写集相交且时间重叠即记为一次应召回冲突）。验收：两 worker 同写一文件的场景在机制测试中被强制串行；合并产物进入验证门；worker 侧无任何写公共日志与记忆的代码路径（静态检查可断言）。

### 4.3 W4：自适应路由（回应 §8.4）

2.0 承认 0.35/0.70 的 fast/mid/deep 切点是初值。W4 不追求离线拟合一次最优阈值，而是把阈值替换为在线估计：每轮任务经验证门得到 0/1 成功信号，按路径维护 Beta 后验 P_success(path|s)；路由比较期望效用并用汤普森采样做探索，探索预算受每日费用闸门约束；0.35/0.70 降级为冷启动先验均值。这样"校准"从一次性活动变成持续活动，且失败不会超出预算（详见 5.2 的解析形式）。

### 4.4 W5：净化边界与对抗测试（回应 §8.5）

2.0 说"净化降低风险但不完备"，这正确但不充分——不完备的系统更需要可度量的边界。W5 把 I2 的操作化定义定为：**决策平面可接收的输入集合只是净化函数输出值域的子集，原始用户输入中的可执行载荷（指令性文本、URL、文件名、提示片段）在决策平面不可达**。实现为两类检查：(a) 静态可达性审计——净化函数输出与决策内阁输入之间不存在绕过路径（代码级检查）；(b) 对抗回归集——注入样本库（直接指令注入、间接注入、工具输出投毒、多轮污染）在发布回归集中逃逸数为 0。安全仍是风险降低，但从此风险降低有了回归测试。

### 4.5 W7：记忆生命周期（回应 §8.7）

记忆记录 schema 增加生命周期字段：

```json
{
  "expId": "e-221",
  "claim": "…",
  "domain": { "projectId": "p3", "taskType": "refactor",
              "toolsetHash": "t-7" },
  "source": { "runId": "r-9", "checks": ["correctness", "safety"] },
  "confidence": 0.93,
  "ttl": { "mode": "expire", "at": "2027-03-08" },
  "createdAt": "2026-09-08",
  "lastValidatedAt": "2026-09-08"
}
```

规则：检索时若经验过期或 confidence 低于阈值且当前任务 domain 签名相近，触发轻量反事实重验（重放来源 runId 的检查或直接丢弃）；项目删除或工具集版本变更时，按 domain 作用域作废并向下游传播（凡引用该经验生成的规则同步标记失效）。I3 不变：以上所有写入仍只发生在验证通过并提交公共日志之后。

---

## 5 进化设计 B：三项理论深化

第 4 节的 W1—W7 是工程化转译；本节的三项是 2.0 论证链真正缺环的地方，其中 5.3 发现了一个 2.0 未察觉的内部冲突。

### 5.1 T1：前缀稳定性的形式化与漂移检测

2.0 把"字节级稳定"当作不变量，但没有定义字节级稳定的对象与检测方式。形式化如下。设每轮请求为三段拼接：

```
Req = P_const || L_log || D_volatile
```

- P_const：system prompt、工具 schema、净化后的任务陈述、会话元数据——会话内不变；
- L_log：只追加的事件日志——只允许 append，且仅当轮次验证通过后提交（见 5.3）；
- D_volatile：当前轮草稿、未验证尝试与修复消息——允许自由改写。

前缀缓存命中的充要条件：本次 [P_const || L_log] 与上次逐字节一致。漂移来源有三：非确定性生成（时间戳、随机种子进入 P_const）、日志原地改写、以及验证失败导致的日志截断。对策是运行时断言：每轮请求前计算前缀指纹 fp = hash(P_const || L_log)，与上一轮比对，漂移即告警并记录归因。这样 I1 从设计口号变成可测试机制（漂移率在机制测试中断言为 0）。

跨会话缓存（小米宣称最高 95%，R2）依赖更强的条件：P_const 必须是"会话确定性模板"——不含任何会话专属状态。日期、用户名、上一任务摘要等可变内容不得进入 P_const，而应作为事件进入 L_log 尾部。这个拆分同时解释了 2.0 文档变化对照表中"同会话 99% / 跨会话 95%"这类差距的来源（R2），并把跨会话命中从运气变成设计约束。

### 5.2 T2：缓存—成本解析模型与自适应路由

2.0 的成本公式只有单段命中率 h。按 5.1 的三段结构重写。设未命中输入单价 p_miss、命中价格系数 alpha（命中价 = alpha·p_miss）、三段输入量 N_const、N_log、N_vol，跨会话命中率 h_c（仅作用于 N_const）、同会话命中率 h_s（作用于 N_log），则单轮输入成本：

```
C_in = p_miss · [ N_vol
                  + N_const · (1 − (1 − alpha)·h_c)
                  + N_log · (1 − (1 − alpha)·h_s) ]
```

两个推论。第一，节省量 = p_miss·(1 − alpha)·(N_const·h_c + N_log·h_s)，随轮次增长主要来自 N_log 单调增——**长会话经济性来自"提交过的日志被命中"，而非来自重发易失草稿**；这从解析上支持了两段式日志（5.3）：草稿写得再多也不产生缓存收益，只有提交进 L_log 的内容才积累收益。第二，跨会话收益的上限由 N_const/N 占比决定；若 P_const 膨胀（例如把工具 schema 全文每轮塞入），即便 h_c=1 也会拖累成本与首 token 延迟——这是对"无限加 system prompt"的定量反对理由。

路由决策沿用 2.0 的期望效用，但把成功概率写成可更新量：

```
E[U_deep] − E[U_fast] = (P_deep − P_fast)·V − (C_deep − C_fast)
```

当 P_deep ≈ P_fast 时走 fast；仅当质量差异显著或任务带高风险标记时走 deep。W4 的在线估计：每路径维护 Beta(a, b)，验证门结果作为观测更新；选择路径时以"后验均值收益 + 汤普森采样"决定，探索开销计入 C_deep/C_fast 并由每日闸门封顶。缓存命中率 h 本身也可观测：从 Provider usage 回传的缓存读取 token 与总输入 token 计算，EWMA 平滑后显示在右侧洞察面板——2.0 的"常驻用量统计"由此获得真实数据源，并在真实 API 接入后把 h 从 R1 本地模拟升级为线上测量。

### 5.3 T3：验证门与只追加日志的一致性协议（2.0 未察觉的冲突）

这是本版最重要的机制发现。2.0 的 L6 同时要求两件事：事件日志只追加（为了前缀稳定）；验证失败可以回退重试。二者存在冲突：若失败轮次的追加已进入日志，回退就必须截断日志；而截断使 [P_const || L_log] 不再与前次请求一致，前缀缓存从截断点起全部失效——重试不但要重做任务，还要重付被截断段的输入成本。**缓存优先与可回退性在单段日志上互斥。**

解法是把日志分成两段，与数据库预写日志（WAL）的提交语义同构：

- 公共日志 L_committed：只接收验证通过的轮次；每轮结束写入提交点 checkpoint；
- 草稿分支 B_draft：容纳失败/修复中的轮次追加，带 branchId，可整体丢弃。

失败回退只丢弃 B_draft，重试请求的前缀仍为 [P_const || L_committed]，字节级稳定不被破坏，缓存收益保留。artifact 版本契约（W2）遵守同一规则：只有 state=committed 的版本进入版本链，rollbackTarget 只在 committed 版本之间移动。由此，"验证后提交"从记忆治理（I3）扩展到日志与成果的全局规则，三条不变量第一次在机制层面互相咬合：I1（前缀稳定）由提交点保证，I3（只写验证过的经验）由提交条件保证，而回退不再牺牲 I1。

同一逻辑还解释了验证门的成本结构：验证失败越晚发现，被丢弃的草稿分支越大，缓存损失与重做成本越高——因此验证应尽量前移（每轮内尽早跑确定性检查），这正是 W2/W3 中"确定性检查优先于 LLM 终审"的原因。验证门自身的可靠性（2.0 未提）用分层缓解：确定性检查（编译、lint、测试、diff 完整性、价格闸门一致性）零 LLM、全脚本化；LLM 检查仅覆盖语义项（正确性、忠实性、安全性判断），固定提示、温度 0、输出 schema 约束，并对约 10% 样本做双模型交叉复核；同时为验证门建立元指标（接受集回归率、拒绝集假阳性率），因为 2.0 第 7.2 节的指标只测了任务完成率，没有测"验证器本身的误判率"。

---

## 6 进化设计 C：证据分级纪律 I4 与可复现实验协议

### 6.1 第四条不变量 I4

**I4 证据分级纪律：论文与产品中的每一个定量主张都必须标注证据级别 R0—R3，并指向可复现来源；无标注的数字不得进入论文正文或产品文案。**

级别定义：

- R0：已实现并通过机制测试（本地可复现，含测试命令）；
- R1：受控实验或可复现测量（本地模拟，或真实 API 上按可复现协议执行的实验；如 2.0 中约 91% 的缓存命中率为本地模拟，在真实 API 上复现前不得表述为真实线上收益）；
- R2：官方转述或厂商宣称（如 78.6 SWE-Bench Verified、97.0% 工具成功率、99%/95% 缓存宣称、价格页数值——来源可追溯、结果未经独立复现）；
- R3：设计推演或架构论证（不可直接实验，如本文 5.1—5.3 的全部新机制）。

I4 是 2.0 三条不变量之上的治理不变量：I1—I3 管系统的运行行为，I4 管系统与论文如何描述自己。它直接回应 §8.1 的"证据偏工程设计"：问题不是"本地模拟"存在，而是本地模拟与厂商宣称在文本中没有被区分。执行方式：正文首次出现每个定量主张时标注级别（本文已示范）；文档级"主张账本"（claim ledger）逐条登记主张、级别、来源、可复现步骤与复现状态。

### 6.2 主张账本（claim ledger）模板

**表 2：主张账本模板示例**

| 主张 | 级别 | 来源 | 可复现步骤 | 状态 |
| --- | --- | --- | --- | --- |
| 前缀指纹漂移率 = 0 | R0 目标 | 本文 5.1 | 机制测试：注入时间戳/截断后断言告警 | 待实现 |
| 两段式日志回退不损失缓存 | R0 目标 | 本文 5.3 | 机制测试：回退后 fp 不变 | 待实现 |
| 约 91% 同会话缓存命中 | R1 | 2.0 §8.1 自述 | 本地模拟复跑 | 已承认非线上 |
| MiMo-V2.5-Pro 缓存价 0.025 元/M | R2 | 2.0 转述官方价格页 | 复核官方页面 | 转述 |
| X 预览模型能力优于默认模型 | — | 无公开契约 | 无 | 不成立，不录入 |

### 6.3 八坐标实验卡（W6）

任何与 MiMo 模型的对照实验必须同时固定八项坐标并在结果旁列出：模型标识与版本、端点/报告日期、system prompt 哈希、工具 schema 哈希、缓存状态（冷/热及命中率）、采样参数（温度、top-p、思考模式开关）、任务集与工具集版本、审批与安全策略。结论仅在坐标一致时可比；坐标不一致的分数对照（例如拿厂商默认配置的 78.6 对比本地自定义提示词的本地分）在论文中禁止出现。厂商报告值在复现前一律标注"R2 外部参考值"。

### 6.4 对 2.0 关键表述的重标注示例

- "同会话缓存命中率最高 99%、跨会话 95%"（小米官方宣称）→ R2。
- "91% 左右缓存命中结果"（深思内阁本地）→ R1，并明确"本地模拟，非线上收益"。
- "0.35/0.70 切点"→ R3 初值，3.0 起由 W4 在线校准替代。
- 本文 5.1—5.3 的全部新机制 → R3 推演，验收标准见 W2—W5 的机制测试清单。

---

## 7 更新的产品指标与验证计划

在 2.0 第 7.2 节产品指标表的基础上，3.0 为每项指标补充证据级别与测量方式，并新增对验证门与日志一致性的元指标：

**表 3：3.0 更新的产品指标、证据级别与测量**

| 目标 | 指标 | 3.0 初始目标 | 级别与测量 |
| --- | --- | --- | --- |
| 缓存经济 | 同会话缓存输入比例 | 本地模拟 ≥90%（R1）；真实 MiMo 接入后按可复现协议线上测量 | R1（本地）→R1（真实 API） |
| 前缀稳定 | 前缀指纹漂移率 | 机制测试 0 | R0 |
| 回退一致性 | 回退后前缀指纹不变 | 机制测试 0 | R0 |
| 任务质量 | 通过验证的任务完成率 | 精选代码集 ≥85% | R1 |
| 工具可靠性 | 合法工具调用成功率 | ≥95% | R1 |
| 安全性 | 注入逃逸次数（对抗回归集） | 发布回归集 0 | R0 |
| 验证门元指标 | 接受集回归率 / 拒绝集假阳性率 | 回归率 ≤2%，假阳性率 ≤10% | R1（新增） |
| 记忆质量 | 验证记忆抽样精确率 | ≥95% | R1 |
| 记忆生命周期 | 过期/失效经验命中后被丢弃率 | 机制测试 100% | R0（新增） |
| 成本治理 | 未授权预算超支率 | 0 | R0 |
| 成果可靠性 | committed 版本回退成功率 | ≥99% | R0 |
| 并行治理 | 合并冲突检测召回率 | ≥95% | R0 |

机制验证（对照 2.0 §7.1）新增：前缀指纹漂移测试、两段式日志提交/回退测试、artifact v1 状态机与原子回退、并行 worker 写集互斥与合并重验、记忆 TTL 与反事实重验、验证门元指标统计、X 预览模型端点/usage/价格契约测试（保持 2.0 的 M0 门：契约公开并过测才可进入默认路由，R2→R0）。

---

## 8 局限与前瞻

### 8.1 3.0 仍是设计研究

本文全部新增机制（两段式日志、指纹检测、自适应路由、分层验证、记忆生命周期）均处于 R3 级，尚未在 mimo-agent 上实现并跑通机制测试。任何读者都不应把本文的公式与协议当作已测量收益；它们是有验收标准的待实现设计。这是 I4 对本文自身的约束，也是与 2.0 相比唯一诚实的立场——2.0 的问题是没说清哪些是测过的，3.0 的问题是还没测，但每个待测项都写明了怎么测。

### 8.2 对小米的引用仍然全部是转述

MiMo-X 预览模型无公开 API 契约、稳定价格与缓存字段，因此凡涉及其"能力"的表述都不可能进入 R1 以上；只要该状态不变，任何深思内阁与 MiMo-X 的定量比较在本文的协议下都无法成立。这不是回避比较，而是把"不可比"显式化为协议结论。

### 8.3 四项前瞻判断

- 近期（3—6 个月）：最有价值的不是增加模型，而是把本文 W2/W3/T1/T3 的机制测试跑绿，让两段式日志、artifact v1、并行契约从前缀假设变成 R0 事实；同时接入真实 MiMo 稳定 API 采集 usage，把 h 从 R1 模拟换成线上测量。
- 中期（6—18 个月）：路由从静态角色映射走向能力图谱与在线校准（W4）；记忆生命周期（W7）在真实多项目数据上检验陈旧经验与项目混淆率；X 预览模型在契约稳定且 M0 通过后进入灰度。
- 长期（18 个月以上）：桌面 Agent 走向具有计划、记忆、工具、成果状态与权限治理的个人操作系统。届时竞争点仍是长任务成功率、可恢复性、成本/质量 Pareto 前沿与信任边界——而 3.0 的论点之一是：信任不只来自模型能力，还来自系统（与论文）对自身证据边界的诚实披露。
- 元层面：4.0 的入口已由本文第 3 节定义——对 3.0 执行同样的自省循环，逐条回应 3.0 的自陈局限（8.1—8.2），并把 W1—W7 中仍停留在 R3 的设计推向 R0/R1。

---

## 9 结论

深思内阁 3.0 是 2.0 的自我进化，进化方式不是扩充功能清单，而是把 2.0 自陈的七条缺点转译为七项带验收标准的工作项（W1—W7），补齐三处论证缺口（T1 前缀稳定形式化、T2 缓存成本解析与自适应路由、T3 验证门与日志一致性），并把证据分级纪律升格为第四条不变量 I4。

进化中最有价值的单一发现是 2.0 的内部冲突：只追加日志与失败回退互斥，回退会摧毁前缀缓存收益；两段式日志（公共提交日志 + 草稿分支）以数据库 WAL 同构的方式化解冲突，使"缓存优先"与"验证后提交"首次在机制层面咬合。这个发现也示范了本文的方法论主张：自我进化的价值主要不在新增，而在发现已有设计中互相矛盾的前提并给出可验证的解法。

与 Xiaomi MiMo Desktop 的对照在 3.0 中被置于证据纪律之下：小米的桌面任务覆盖与端到端成果工作流仍是产品基准，深思内阁的缓存经济学、输入隔离、验证治理与证据披露仍是工程约束基准，但两者的一切定量比较都必须先通过八坐标实验卡，一切厂商数字都停留在 R2，直到独立复现。深思内阁的长期主张不变——在长任务 Agent 中，上下文组织、权限治理、成本可观测性、成果可回退性与验证边界，与模型能力同等重要；3.0 的增量主张是：**系统对自身证据边界的披露能力，也应被视为同等重要的系统属性。**

---

## 参考文献

1. LY. 《深思内阁 2.0：面向前缀缓存与验证治理的编程智能体架构——与 Xiaomi MiMo Desktop 及 MiMo-X 预览模型的比较研究》，2026-09-08（本文的进化基线，本地文档 D1；其中对小米的转述均按 R2 处理）。
2. Xiaomi MiMo. Xiaomi MiMo Desktop open beta and product matrix. https://mimo.mi.com （转述，访问日期 2026-09-08）。
3. Xiaomi MiMo. Xiaomi MiMo Desktop open beta. https://mimo.xiaomimimo.com/desktop/invite/ （转述，访问日期 2026-09-08）。
4. Xiaomi MiMo. Model updates and release notes. https://mimo.mi.com/docs/en-US/updates/model （转述，访问日期 2026-09-08）。
5. Xiaomi MiMo. Model summary and pricing. https://mimo.mi.com/docs/en-US/quick-start/summary/model；https://mimo.mi.com/docs/price/pay-as-you-go （转述，访问日期 2026-09-08）。
6. Anthropic. Building effective agents. 2024.
7. Yao, S., et al. ReAct: Synergizing Reasoning and Acting in Language Models. ICLR, 2023.
8. Du, Y., et al. Improving Factuality and Reasoning in Language Models through Multiagent Debate. ICML, 2024.
9. Shinn, N., et al. Reflexion: Language Agents with Verbal Reinforcement Learning. NeurIPS, 2023.
10. Jimenez, C. E., et al. SWE-bench: Can Language Models Resolve Real-World GitHub Issues? ICLR, 2024.
11. Lewis, P., et al. Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks. NeurIPS, 2020.
12. Liang, P., et al. Holistic Evaluation of Language Models (HELM). TMLR, 2023.（证据分级与多维度评测方法参考）
